# Mini-Project Report (Draft — 100–150 words)

Fill in the bracketed details, then trim/expand to fit the 100–150 word limit.

---

Our group developed a binary image classifier to distinguish **deer** from
**antelope**, using [X] training images and [Y] validation images sourced from
[state your dataset source, e.g., Kaggle wildlife datasets / manually collected
web images]. The model uses transfer learning with a frozen MobileNetV2
backbone, fine-tuned on our dataset, achieving [Z]% validation accuracy. The
app is built with Streamlit: users upload an image and receive a predicted
label with a confidence score. Deployment used Streamlit Community Cloud,
linked to our GitHub repository.

The main challenges were [e.g., limited/imbalanced training data causing
overfitting, distinguishing visually similar species, large model file size
during deployment]. We addressed these through data augmentation, fine-tuning,
and [Git LFS / reducing image resolution] for deployment. Future improvements
could include a larger, more diverse dataset and additional regularization.

---

Word count check: paste your final text into a word counter before submission.
